% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feat.R
\name{build_tt_env}
\alias{build_tt_env}
\title{Combine Typst features or raw strings into preamble and epilogue strings}
\usage{
build_tt_env(...)
}
\arguments{
\item{...}{Multiple lists returned by feat_* functions or raw character strings.}
}
\value{
A list with named elements 'preamble' and 'epilogue' for use in tt().
}
\description{
Stitches together multiple Typst rules. Character strings are
treated as preambles. Preambles are concatenated in order; epilogues are
reversed to ensure nested blocks close correctly (LIFO).
}
\examples{
\dontrun{
# Example usage in a Quarto document:
env <- build_tt_env(
  feat_env(font_size = "8pt", align = "center"),
  "#set table(stroke: 0.5pt + blue)\n",
  feat_clip(),
  feat_notes("Source: xyz")
)

df \%>\% tt(preamble = env$preamble, epilogue = env$epilogue)
}
}
